extends ./layout

block body
  .views
      .view.view-main.layout-white
        .navbar
          .navbar-inner
            // We need cool sliding animation on title element, so we have additional "sliding" class
            a.left(href='index-directory.html')
              img(src='/img/logo.png', alt='logo')
            -if(isHasMail == "true")
              a#nextSetp.right(href='templates/finish.html', data-context="" disabled) next
            -else
              a#nextSetp.right(href='templates/finishMail.html', data-context="" disabled) next
        .pages
          .page.navbar-fixed(data-page='stroryHobby')
            .page-content
              form#stroryHobbyChoose.store-data
                include ./hobbyItems.jade
  block scripts
